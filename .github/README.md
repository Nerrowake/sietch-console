# GitHub Configuration

Sietch Console uses Nerrowake organization defaults for issue templates, pull
requests, conduct, support, and security guidance unless repository-specific
workflows are added here.

This repository currently documents a `development` branch workflow in
`CONTRIBUTING.md`. That branch model is intentionally preserved until the project
chooses to standardize on direct `main` pull requests.
